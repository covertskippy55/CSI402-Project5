/* Name:Dilan Samarasinghe
   ID: DS697678
*/

#define NUM_ARGS_C 4 // if the flag is c we need 4 arguments atleast
#define NUM_ARGS_X 3 // if the flag is x we need 3 arguments atleast
#define FLAG 1 // where the flag is in argv[]
#define ARCHIVE_NAME 2 // where the archive name is in argv[]
#define LINE_SIZE 512 // an optemized number of bytes to be read from a file
#define MAX_FILE_LENGTH 256 // max file name length allowed by unix normally
#define NUMTOREMOVE 3 // this is the number to remove from argc
